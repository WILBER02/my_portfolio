# assets/resume — drop your resume here

Drop `Wilberforce_Muramba_Resume.pdf` into this folder.

The hero "Download Resume" button already points to
`assets/resume/Wilberforce_Muramba_Resume.pdf` with the `download` attribute,
so the file will be downloadable as soon as you add it.

- If you use a different filename or extension, update the link in `index.html`
  (search for `Wilberforce_Muramba_Resume.pdf`).
- Keep it single-page and keyword-matched to the portfolio (see README → Recruiter notes).
